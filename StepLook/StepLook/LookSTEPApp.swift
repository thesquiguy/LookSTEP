import SwiftUI

@main
struct LookSTEPApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
                .frame(minWidth: 720, minHeight: 500)
        }
        .defaultSize(width: 1_080, height: 760)
    }
}
